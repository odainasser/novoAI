namespace Domain.Enums;

public enum NotificationType
{
    RequestApproved = 1,
    RequestRejected = 2,
    LowStock = 3,
    StocktakeReview = 4,
    PurchaseRequestSubmitted = 5,
    PurchaseRequestProposed = 6
}
