@* Replaced by /admin/account/change-password — kept for backward compat *@
@page "/admin/change-password-legacy"
@attribute [Authorize]
@using Microsoft.AspNetCore.Authorization
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/admin/account/change-password", replace: true);
    }
}

