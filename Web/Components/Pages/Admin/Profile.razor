@* Replaced by /admin/account — kept for backward compat *@
@page "/admin/profile-legacy"
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/admin/account", replace: true);
    }
}
