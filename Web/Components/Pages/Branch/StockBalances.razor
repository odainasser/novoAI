@page "/branch/stock-balances"
@layout BranchLayout
@attribute [Authorize]
@using Microsoft.AspNetCore.Authorization
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Legacy route — consolidated into the tabbed /branch/inventory page.
        Navigation.NavigateTo("/branch/inventory/balances", replace: true);
    }
}
